{
  "name": "genome-hub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/client",
    "packages/server",
    "packages/infra"
  ],
  "scripts": {
    "dev":     "concurrently -n client,server -c cyan,green \"npm run dev -w packages/client\" \"npm run dev -w packages/server\"",
    "build":   "npm run build -w packages/shared && npm run build -w packages/client && npm run build -w packages/server",
    "deploy":  "npm run build && npm run deploy -w packages/infra",
    "db:up":   "docker-compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
